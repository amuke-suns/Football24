name: football_news
description: A Flutter project for learning about APIs (specifically, api-football-beta API)

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.4
  json_annotation: ^4.1.0
  flutter_svg: ^1.0.3
  grouped_list: ^4.2.0
  provider: ^6.0.0
  google_fonts: ^2.2.0
  shared_preferences: ^2.0.13
  intl: ^0.17.0
  auto_route: ^3.2.4
  get_it: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: ^2.1.2
  json_serializable: ^6.1.5
  auto_route_generator: ^3.2.3

flutter:

  uses-material-design: true

  assets:
    - assets/
